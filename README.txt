СЕРВИС.COM — ЭТАП 2: НОВАЯ СТРАНИЦА КАТЕГОРИИ (эталон — Видеонаблюдение)
=======================================================================
Деплой на ВЕБ-сервис. Импорт не нужен.

ЧТО ЭТО:
  Раздел «Видеонаблюдение» переведён на новый дизайн (как в макете):
  новый header + мегаменю, хлебные крошки, H1, подкатегории (чипсы),
  фильтры-фасеты слева (бренд с поиском + тип + разрешение + цена +
  «только в наличии», со счётчиками, мультивыбор, сброс; на мобильном —
  кнопка «Фильтры»), сортировка, новые карточки товара (артикул,
  характеристики, наличие, цена, «+ В заявку», WhatsApp, избранное,
  сравнение), «Показать ещё», и корзина-заявка (панель справа,
  отправляет в базу через /api/order с телефоном/комментарием).

ФАЙЛЫ (4):
  server.js                    — на случай, если на Этапе 1 не обновился
                                 (даёт ?group= и /api/home; заодно оживит
                                 блоки категорий/хитов/брендов на главной).
  public/css/catalog.css       — НОВЫЙ. Стили страницы категории.
  public/js/catalog.js         — НОВЫЙ. Фасеты + карточки + пагинация +
                                 корзина + мегаменю.
  public/videonablyudenie.html — перевёрстан на новый дизайн.

ВАЖНО: это ЭТАЛОН одного раздела. Остальные разделы (Сетевое, СКУД,
Пожарная) пока в старом виде — размножу на них после твоего «ок», плюс
добавлю страницы ИБП и «Кабельные».

ДЕПЛОЙ:
 1) GitHub → Upload files → server.js (в корень) + папку public/ целиком
    (ВКЛЮЧАЯ новые public/css/catalog.css и public/js/catalog.js) → Commit.
 2) Render → веб-сервис → Manual Deploy → Clear build cache & deploy.
 3) Открой /videonablyudenie.html с Cmd+Shift+R.

ПРОВЕРЬ:
 [ ] Новый header, «Каталог» открывается плитками.
 [ ] Слева фильтры со счётчиками; отметь бренд/разрешение — список и
     счётчики пересчитываются; «Сбросить» очищает.
 [ ] Поиск бренда внутри фильтра.
 [ ] Сортировка меняет порядок; «Показать ещё» догружает.
 [ ] «+ В заявку» на карточке → открывается панель справа; количество,
     удаление; телефон + «Отправить заявку» → заявка уходит (проверь в
     админке, в заявке будет услуга «Видеонаблюдение» и страница).
 [ ] На телефоне: кнопка «Фильтры» открывает панель фильтров.
