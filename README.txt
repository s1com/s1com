СЕРВИС.COM — ЭТАП 1: НОВАЯ ГЛАВНАЯ + МЕГАМЕНЮ (по утверждённому макету)
=====================================================================
Деплой на ВЕБ-сервис. Импорт не нужен.

ФАЙЛЫ (3):
  server.js          — /api/home (категории со счётчиками, бренды, хиты,
                       новинки) + поиск ?q= и фильтр ?group= в /api/products.
  public/index.html  — НОВАЯ главная-лендинг: мегаменю (плитки), герой с
                       поиском, сегменты, карточки категорий, хиты, новинки,
                       бренды, доверие, CTA. Самодостаточна (свои стили).
  public/js/home.js  — данные + мегаменю + живой поиск. НОВЫЙ ФАЙЛ,
                       положить в public/js/.

ЧТО ИЗМЕНИТСЯ:
  • Главная (/) — современный лендинг вместо сетки «все товары».
  • «Каталог» → мегаменю плитками → подкатегории.
  • Поиск в шапке/герое ищет вживую и показывает карточки на главной.
  • Карточки ведут на страницу товара + быстрый WhatsApp.
  • Клик по категории ведёт на текущую страницу раздела (старый вид) —
    её новый вид сделаем следующим этапом.

НЕ ТРОГАЛОСЬ: app.js / style.css / страницы разделов (работают как прежде).
Разделы ИБП и «Кабельные» пока находятся через поиск (страницы добавим на
этапе категории).

ДЕПЛОЙ:
 1) GitHub → Upload files → server.js (в корень) + папку public/ целиком
    (ВКЛЮЧАЯ новый public/js/home.js) → Commit.
 2) Render → веб-сервис → Manual Deploy → Clear build cache & deploy → Live.
 3) Открой / с Cmd+Shift+R. Проверь: мегаменю, поиск («Dahua»), карточки
    категорий со счётчиками, хиты, бренды.
