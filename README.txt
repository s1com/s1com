СЕРВИС.COM — ЭТАП 1: ЛИД-СИСТЕМА (единый архив, ВЕБ-сервис)
==========================================================
Заливать ВСЕ файлы разом (админка+сервер+каталог согласованы). Импорт не нужен.
Включает все прежние изменения + новую лид-систему.

НОВОЕ (Этап 1 — заявки и лиды):
  • Форма заявки: добавлено поле «Комментарий»; при отправке автоматически
    сохраняются УТМ-метки, страница-источник и откуда пришёл клиент.
  • Услуга определяется автоматически по странице (видеонаблюдение/СКУД/
    пожарная/сетевое/карточка товара) — видно в заявке.
  • В админке (Заявки): у каждой заявки видно услугу, страницу, UTM и
    комментарий клиента; добавлен фильтр по статусу
    (Все/Новые/В работе/Заказы/Отказы).
  • Экспорт в Excel дополнен: услуга, страница, UTM, комментарий, сумма.

ФАЙЛЫ В АРХИВЕ:
  server.js, db.js          — приём/хранение полей заявки, обогащённый экспорт.
  public/js/app.js          — поле комментария + захват UTM/страницы/источника.
                              (Каталог оставлен в твоём виде — без мегаменю.)
  admin/index.html          — показ новых полей + фильтр статуса.
  lib/product-page.js       — (из прошлой сборки) WhatsApp+доверие, характеристики.
  public/*.html (5)         — (из прошлой сборки) динамический FAQ, canonical.

НЕ ВКЛЮЧЕНО: style.css (не менялся), scripts/alstyle-import.js (крон, уже работает).

ДЕПЛОЙ:
 1) GitHub → Upload files → в КОРЕНЬ server.js, db.js + папки admin/, lib/,
    public/ (со структурой, включая public/js/app.js) → Commit.
 2) Render → веб-сервис → Manual Deploy → «Clear build cache & deploy» → Live.
 3) Сайт и /admin — Cmd+Shift+R.

ПРОВЕРКА ЗАЯВОК:
 [ ] На сайте: «В заявку» → в форме появилось поле «Комментарий».
 [ ] Отправь тестовую заявку (можно с ?utm_source=test в адресе).
 [ ] Админка → Заявки: заявка на месте, видно услугу/страницу/UTM/комментарий.
 [ ] Фильтр по статусу работает.
 [ ] Кнопка «Экспорт в Excel» скачивает файл с новыми колонками.
